#include <bits/stdc++.h>

using namespace std;

void solve () {
	uint n; cin >> n;
	pair<uint, uint> arr[n];
	for (uint i = 0; i < n; i++) {
		cin >> arr[i].first >> arr[i].second;
	}
}

int main () {
	uint t; cin >> t;
	while (t--) {
		solve();
	}
}