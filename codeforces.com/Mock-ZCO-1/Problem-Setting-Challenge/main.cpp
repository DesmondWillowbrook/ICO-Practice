#include<bits/stdc++.h>

using namespace std;

int main () {
	uint n, k;
	pair<uint, uint> a[n]; pair<uint, uint> b[n];

	for (uint i = 0; i < n; i++) {
		cin >> a[i].first;
		a[i].second = i;
	}
	for (uint i = 0; i < n; i++) {
		cin >> b[i].first;
		b[i].second = i;
	}
}