#include<bits/stdc++.h>

using namespace std;

int min_arr (int* arr, int n) {
	int min_val = INT_MAX;
	for (int i = 0; i < n; i++) {
		if (min_val > arr[i]) {

		}
	}
	return min_val;
}

int main () {
	int t; cin >> t;
	while (t--) {
		int n; cin >> n;
		int arr[n]; 
		for (int i = 0; i < n; i++) {
			cin >> arr[i];
		}
		long int ans = INT_MAX;
		
	}
}