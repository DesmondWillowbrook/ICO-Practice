#include <bits/stdc++.h>

using namespace std;

size_t N, K;

int main () {
    cin >> N >> K;
    long int V[N]; uint B[N];
    for (size_t i = 0; i < N; i++) cin >> V[i];
    for (size_t i = 0; i < N; i++) cin >> B[i];

    
}