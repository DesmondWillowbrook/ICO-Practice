#include<bits/stdc++.h>

using namespace std;



int main () {
    size_t N, Q;
    cin >> N;
    long long int num[N], dp[N];

    for (size_t i = 0; i < N; i++) {
        cin >> num[i]; dp[i] = -1;
    }

    size_t q, ans;
    while (Q--) {
        cin >> q;
        ans = 0;
        for (size_t i = 0; i < q; i++) {

        }
    }
}