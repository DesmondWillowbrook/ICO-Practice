#include<bits/stdc++.h>

using namespace std;

size_t N;

int main () {
    cin >> N;
    uint a [N];

    uint max_ele = 0;
    for (size_t i = 0; i < N; i++) {
        cin >> a[i];
    }

    uint max_ele = 0;
    for (size_t i = 0; i < N; i++) {
        
    }
}