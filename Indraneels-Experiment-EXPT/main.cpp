/*
Problem is to find successive differences
Then find common substring and print the numbers asssociated with them.
=> 2N memory usage
O(N) for calculating successive differences
O(N^2) for find longest common substring
*/

#include<bits/stdc++.h>

using namespace std;

size_t N;

int main () {
    cin >> N;
}